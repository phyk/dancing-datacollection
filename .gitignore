# Ignore data output directory (not tracked by git)
data/
# (Allowed into context for local development and testing)

# Ignore logs output directory (not tracked by git)
logs/
# (Allowed into context for local development and testing)

# Ignore Python cache files
__pycache__/
*.pyc

# Ignore data output directory (not tracked by git)
data/
# (Allowed into context for local development and testing)

# Ignore logs output directory (not tracked by git)
logs/
# (Allowed into context for local development and testing) 